package javapractice.stringclass;

public class StringObjectLiteral {

    /*
        - An object that represents sequence of characters.
        - There are two ways to create String object:
            1. String literal.
            2. using "new" keyword.
        - String objects are immutable.
            Immutable - means once it's created (allocated in the heap) it cannot be modified.

        - String Literals:
            - Created by using double quotes("").
            - Allocated in the special memory in the heap named String pool.
            - An allocation of memory in Java heap memory.
            - If the string already exists in the pool, a reference to the pooled instance is returned.
     */

}
